interface:
  display_name: 1km 대본 작성
  short_description: 기사 호명 없이 사건과 말맛이 살아 있는 대본을 만듭니다.
  default_prompt: Use $1km-write-script to draft an evidence-based conversational story
    that breaks the viewer's first assumption and returns to the opening event.
  icon_small: assets/icon.svg
  icon_large: assets/icon.svg
policy:
  allow_implicit_invocation: true
